import React from "react";
import AverageMetricList from "../components/AverageMetricList";
import Layout from "../components/Layout";

const AverageMetrics = () => {
  return (
    <Layout>
      <AverageMetricList />
    </Layout>
  );
};

export default AverageMetrics;
